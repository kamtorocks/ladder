{
  "name": "ladder",
  "version": "2.0.0",
  "private": true,
  "description": "Clash subscription service on PocketBase",
  "type": "module",
  "scripts": {
    "dev": "pocketbase serve --http=127.0.0.1:8090 --migrationsDir=pb_migrations --hooksDir=pb_hooks",
    "deploy": "sh scripts/deploy.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
